/**
 * Empty hook stub for Socket.IO connection management.
 * Business logic will be implemented in Phase 8+.
 */
export function useSocket() {
  return {
    isConnected: false,
    connect: () => {},
    disconnect: () => {},
  };
}
